name: Test Bad Prompt (CI/CD Blocking Demo)

on:
  workflow_dispatch:  # Manual trigger button in GitHub Actions UI

jobs:
  test-bad-prompt:
    runs-on: ubuntu-latest
    steps:
      - name: Info
        run: |
          echo "🧪 Testing CI/CD blocking with bad prompt"
          echo "This will use a prompt that encourages hallucination"
          echo "The benchmark should FAIL and block the push"

  rotate-with-bad-prompt:
    needs: test-bad-prompt
    uses: ./.github/workflows/rotate-prompt.yml
    with:
      use_bad_prompt: 'true'
    secrets: inherit
